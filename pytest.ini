[pytest]
DJANGO_SETTINGS_MODULE = config.settings.testing
python_files = tests/*/test_*.py tests/*/*/test_*.py
asyncio_mode = auto
addopts =
    --strict-markers
    --tb=short
    -q

markers =
    unit: Pure unit tests (no database, no external services)
    integration: Integration tests (database + Channels)
